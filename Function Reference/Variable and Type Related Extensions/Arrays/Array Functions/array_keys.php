<?php
# array_keys — Return all the keys or a subset of the keys of an array

    $array = array(
        0 => 100,
        "color" => "red"
    );
    $array_keys = array_keys($array);

    print_r($array_keys);
?>
